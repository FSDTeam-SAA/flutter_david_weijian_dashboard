import 'package:flutter/material.dart';

class NavContactScreen extends StatelessWidget {
  const NavContactScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Contact Screen"));
  }
}
