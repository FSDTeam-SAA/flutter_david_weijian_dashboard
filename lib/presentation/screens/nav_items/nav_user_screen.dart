import 'package:flutter/material.dart';

class NavUserScreen extends StatelessWidget {
  const NavUserScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("NavUserScreen"),);
  }
}